# Docker Environment Configuration
# Copy to .env before running docker-compose

NODE_ENV=production
PORT=3000

# MySQL Configuration (matching docker-compose.yml)
DB_NAME=eyun_chatbot
DB_USER=root
DB_PASSWORD=Fy123456
DB_HOST=mysql
DB_PORT=3306
